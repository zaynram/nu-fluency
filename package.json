{
  "name": "nu-fluency",
  "version": "0.0.1",
  "private": true,
  "workspaces": ["server"],
  "scripts": {
    "mcp": "bun --silent --cwd=server",
    "mcp:build": "bun --silent --cwd=server run build",
    "mcp:start": "bun --silent --cwd=server run start",
    "mcp:watch": "bun --silent --cwd=server test --watch"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "bun-types": "^1.3.14",
    "typescript": "^5.0.0"
  }
}
