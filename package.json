{
  "name": "nu-fluency",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "mcp": "bun --silent --cwd=server",
    "mcp:start": "bun --silent --cwd=dist run index.js",
    "mcp:check": "cd server && bunx tsc --noEmit && bun test --bail",
    "mcp:build": "bun --silent mcp run build && cp --no-target-directory --recursive server/build dist",
    "mcp:watch": "bun --silent mcp test --watch",
    "mcp:clean": "rm -rf server/build server/node_modules",
    "mcp:regen": "bun --slient mcp:clean && bun install && bun --silent mcp:build"
  },
  "dependencies": {
    "nushell-mcp": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "bun-types": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
